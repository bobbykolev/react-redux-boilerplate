import React from 'react';
import Card from './Card';

const About = (props) => {
	return (
	  <div>
	    <Card title={'About'}>
            Card
        </Card>
	  </div>
	);
}

export default About;